services:
  nginx:
      build:
        context: ./requirements/Nginx
        dockerfile: Dockerfile
      container_name: nginx
      ports:
        - "443:443"
      networks:
        - default
      env_file: 
        - .env
      volumes:
        - ~/data/wp:/var/www/html #look here for site files
      healthcheck:
        test: ["CMD", "nginx", "status"]
        interval: 5s
        timeout: 60s
        retries: 50
      restart: always